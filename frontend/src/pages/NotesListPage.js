import React, {
    useState,
    useEffect
} from 'react'

const NotesListPage = () => {

    let [notes, setNotes] = useState([])

  return (
    <div>
        notes
    </div>
  )
}

export default NotesListPage